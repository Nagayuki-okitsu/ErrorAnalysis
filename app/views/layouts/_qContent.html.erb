<div class="con_box">

    <div class="box-title">質問内容</div>
    <p><%= @question.content %></p>

    <% @image_list.each do |img| %>
      <div class="center">「<%= img["name"] %>」</div>
      <div class="center"><%= image_tag(img["path"],{class: "con_img"}) %></div>
    <% end %>
    
</div>